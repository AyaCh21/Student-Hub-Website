{% extends 'base.html.twig' %}

{% block content %}

    {{ form_start(form_rate_prof) }}
    {{ form_errors(form_rate_prof) }}

    {{ form_row(form_rate_prof.student) }}
    {{ form_row(form_rate_prof.professor) }}
    {{ form_row(form_rate_prof.rate) }}
    {{ form_row(form_rate_prof.save) }}
    {{ form_end(form_rate_prof) }}

{% endblock %}