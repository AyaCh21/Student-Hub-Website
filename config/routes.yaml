controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

#every time has a new route, add to here? the extra domains and the ones not defined in the controller
home:
    path: /
    controller: App\Controller\CouBooksController::homePage